class Traveler {
    constructor(travelerData) {
      this.id = travelerData.id;
      this.name = travelerData.Username;
      this.travelerType =travelerData.travelerType
    };
};


export default Traveler
